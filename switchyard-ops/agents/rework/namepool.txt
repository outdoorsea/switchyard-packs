# Railway maintenance-of-way occupations — themed session names for the
# rework pool. Kin to brakeman/namepool.txt (the builders), but drawn from the
# roles that REPAIR what is already laid: the yard's maintenance crews.
#
# `gc` draws one name per concurrent session, so this pool must hold at least
# max_active_sessions entries (rework runs one). Keep them single-word and
# lowercase — a name is a session id, not a specialty.
trackman
wrightman
gandydancer
carman
